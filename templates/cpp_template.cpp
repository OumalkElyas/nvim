#include<bits/stdc++.h>
using namespace std;

#ifdef OE
#define deb(x) cout << #x << ": " << x << "\n";
#else
#define deb(x) (void)0
#endif

void solve(){
	
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t; cin >> t;
    while(t--) 
	solve();

    return 0;
}
