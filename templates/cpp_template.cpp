#include<bits/stdc++.h>
using namespace std;
using ll = long long;

#ifdef OE
#define deb(x) cout << #x << ": " << x << "\n";
#else
#define deb(x) (void)0
#endif

void solve(){
	
}

int main(){
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int T; std::cin >> T;
    while(T--) 
	solve();

    return 0;
}
